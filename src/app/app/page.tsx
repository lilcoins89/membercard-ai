"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { ArrowRight, CreditCard, Package, Plus, Sparkles, Truck } from "lucide-react";
import { Button } from "@/components/ui/button";
import { MembershipCard } from "@/components/card/MembershipCard";
import type { CardDraft } from "@/lib/types";

export default function DashboardPage() {
  const [card, setCard] = useState<CardDraft | null>(null);
  useEffect(() => { try { const saved = sessionStorage.getItem("mc_card"); if (saved) setCard(JSON.parse(saved)); } catch { setCard(null); } }, []);
  return <main className="min-h-dvh bg-background"><header className="mx-auto flex max-w-6xl items-center justify-between px-5 py-6 lg:px-8"><Link href="/" className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-xl bg-primary text-sm font-semibold text-primary-foreground">M</span><span className="font-display text-xl">MemberCard AI</span></Link><div className="flex items-center gap-2"><Button asChild variant="outline" size="sm"><Link href="/shipments"><Truck className="size-4" /> Shipments</Link></Button><Button asChild size="sm"><Link href="/create"><Plus className="size-4" /> New card</Link></Button></div></header><section className="mx-auto max-w-6xl space-y-8 px-5 pb-16 lg:px-8"><div><p className="text-sm text-primary">Workspace overview</p><h1 className="mt-2 font-display text-4xl tracking-tight md:text-5xl">Your membership desk</h1><p className="mt-3 max-w-xl text-muted-foreground">Create, manage, and deliver the cards that make your community feel real.</p></div><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-border bg-card p-5"><Sparkles className="size-5 text-primary" /><p className="mt-5 text-3xl font-semibold">{card ? "01" : "00"}</p><p className="mt-1 text-sm text-muted-foreground">Cards created</p></div><div className="rounded-2xl border border-border bg-card p-5"><Package className="size-5 text-primary" /><p className="mt-5 text-3xl font-semibold">00</p><p className="mt-1 text-sm text-muted-foreground">Shipments placed</p></div><div className="rounded-2xl border border-border bg-card p-5"><CreditCard className="size-5 text-primary" /><p className="mt-5 text-3xl font-semibold">Ready</p><p className="mt-1 text-sm text-muted-foreground">Workspace status</p></div></div><div className="grid gap-6 lg:grid-cols-[1.15fr_.85fr]"><section className="rounded-2xl border border-border bg-card p-5 md:p-7"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[.16em] text-muted-foreground">Latest card</p><h2 className="mt-2 font-display text-2xl">{card ? card.organizationName : "Your next membership card"}</h2></div>{card && <span className="rounded-full bg-accent px-3 py-1 text-xs text-accent-foreground">Ready</span>}</div>{card ? <MembershipCard model={{ organizationName: card.organizationName, memberName: card.memberName, memberNumber: card.memberNumber, membershipType: card.membershipType, expiration: card.expiration, photoUrl: card.photoDataUrl, status: "active", design: card.design }} className="mx-auto mt-7" /> : <div className="mt-7 rounded-xl border border-dashed border-border p-10 text-center"><Sparkles className="mx-auto size-8 text-primary" /><p className="mt-3 font-medium">No card created yet</p><p className="mt-1 text-sm text-muted-foreground">Use the AI studio to make your first card.</p><Button asChild className="mt-5"><Link href="/create">Create with AI <ArrowRight className="size-4" /></Link></Button></div>}</section><aside className="space-y-4"><div className="rounded-2xl border border-border bg-primary p-6 text-primary-foreground"><p className="text-xs uppercase tracking-[.16em] opacity-70">Quick start</p><h2 className="mt-3 font-display text-3xl">Build a card your members want to keep.</h2><Button asChild variant="secondary" className="mt-6"><Link href="/create">Open card studio <ArrowRight className="size-4" /></Link></Button></div><div className="rounded-2xl border border-border bg-card p-6"><p className="text-xs uppercase tracking-[.16em] text-muted-foreground">Delivery center</p><h2 className="mt-2 font-display text-2xl">Track physical cards</h2><p className="mt-2 text-sm leading-relaxed text-muted-foreground">Review addresses, payment state, and fulfillment progress in one place.</p><Button asChild variant="outline" className="mt-5 w-full"><Link href="/shipments">View shipments <ArrowRight className="size-4" /></Link></Button></div></aside></div></section></main>;
}
